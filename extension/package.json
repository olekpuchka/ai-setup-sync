{
  "name": "ai-setup-sync",
  "displayName": "AI Setup Sync",
  "description": "Sync AI setup files — Claude Code, GitHub Copilot, Cursor, Google Antigravity, OpenAI Codex, and more — across every project from a shared GitHub repository, automatically.",
  "version": "1.7.0",
  "publisher": "olekpuchka",
  "author": {
    "name": "Olek Puchka"
  },
  "license": "MIT",
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#000000",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "AI"
  ],
  "keywords": [
    "ai setup",
    "claude code",
    "claude sync",
    "copilot",
    "cursor",
    "antigravity",
    "codex",
    "agents",
    "skills",
    "instructions"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/olekpuchka/ai-setup-sync.git"
  },
  "homepage": "https://github.com/olekpuchka/ai-setup-sync",
  "bugs": {
    "url": "https://github.com/olekpuchka/ai-setup-sync/issues"
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "aiSetupSync.syncNow",
        "title": "Sync Now",
        "category": "AI Setup Sync"
      },
      {
        "command": "aiSetupSync.removeSyncedFiles",
        "title": "Remove Synced Files",
        "category": "AI Setup Sync"
      },
      {
        "command": "aiSetupSync.setGitHubToken",
        "title": "Set GitHub Token",
        "category": "AI Setup Sync"
      }
    ],
    "configuration": {
      "title": "AI Setup Sync",
      "properties": {
        "aiSetupSync.repository": {
          "order": 1,
          "type": "string",
          "default": "",
          "markdownDescription": "GitHub repository URL to sync setup files from, e.g. `https://github.com/your-org/your-repo`. GitHub Enterprise Server URLs are also supported. Private repos, SSO orgs, and Enterprise Server require a **classic** token with the `repo` scope — [Set GitHub Token](command:aiSetupSync.setGitHubToken)."
        },
        "aiSetupSync.branch": {
          "order": 2,
          "type": "string",
          "default": "main",
          "markdownDescription": "Branch to sync setup files from. Defaults to `main` — set to `master` or any other branch name if your repo uses a different default."
        },
        "aiSetupSync.targetFolders": {
          "order": 3,
          "type": "object",
          "additionalProperties": {
            "type": "boolean"
          },
          "default": {
            ".claude": true,
            "CLAUDE.md": true,
            ".mcp.json": true,
            ".github": true,
            ".vscode/mcp.json": true,
            ".cursor": true,
            ".cursorignore": true,
            ".cursorindexingignore": true,
            ".codex": true,
            ".agents": true,
            "AGENTS.md": true,
            ".antigravity.md": true
          },
          "markdownDescription": "Files and folders to sync from the repo root. Toggle entries on or off, or add custom paths for any tool that reads config from your project. Defaults cover Claude Code, GitHub Copilot, Cursor, Google Antigravity, and OpenAI Codex, including their MCP configs."
        },
        "aiSetupSync.pathMappings": {
          "order": 4,
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "markdownDescription": "Rename paths as files are synced from the repo to your project. `\"Claude\": \".claude\"` rewrites `Claude/instructions/style.md` → `.claude/instructions/style.md`. More specific (longer) keys always win. Leave empty if your repo already uses the paths AI tools expect. [Learn more](https://github.com/olekpuchka/ai-setup-sync/blob/main/extension/README.md#settings)"
        },
        "aiSetupSync.postSyncCommand": {
          "order": 5,
          "type": "string",
          "scope": "resource",
          "default": "",
          "markdownDescription": "Shell command to run after a sync changes files — e.g. `npm run generate` to build configs from synced templates. Runs only in [trusted workspaces](https://code.visualstudio.com/docs/editor/workspace-trust); output goes to the **AI Setup Sync** log. Leave empty to disable."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "vscode:uninstall": "node ./dist/uninstall.js",
    "clean": "rm -rf dist",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npm run clean && cp ../CHANGELOG.md . && tsc -p ./",
    "lint": "tsc -p ./ --noEmit",
    "vsce:package": "vsce package --no-dependencies --out /tmp/ai-setup-sync.vsix"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.0.0",
    "typescript": "^6.0.3"
  }
}
